Load flowchart from JSON file
    Put node references in connection objects (findById)
Save flowchart to JSON
    Give each node an ID
    Have connections reference node IDs when saving
Keyboard controls
    Esc to exit node naming / renaming prompt
    Backspace / delete node / connection
    Arrow key / enter navigation through context menu
Undo
Resize node
Node styles
Connection styles
Display information when a node is selected